"""Artifact summary helpers.

NOTE: this package was previously swallowed by a bare ``artifacts/``
pattern in .gitignore and existed only on the dev host. Reconstructed
2026-07-02 from its call sites; the ignore pattern is now root-anchored.
"""
