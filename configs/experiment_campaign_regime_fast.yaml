experiment_name: campaign_regime_on_off_fast
base_orchestration_config_path: configs/orchestration_experiment_fast.yaml
output_root_dir: artifacts/experiments_fast
run_label_metadata:
  campaign: fast_local_debug
  question: regime
notes: Fast local-debug regime comparison. Uses the small promotion_fixture input set and lightweight orchestration stages for functionality checks, not final evidence.
variants:
  - name: regime_on
    feature_flags:
      regime: true
      adaptive: true
      governance: true
  - name: regime_off
    feature_flags:
      regime: false
      adaptive: true
      governance: true
