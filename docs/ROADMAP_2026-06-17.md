# System Improvement Roadmap — 2026-06-17

**State at audit:** equity **$386** (up from $275 on May 22, +40% over ~19 active
days). The engine is profitable when it runs. Three structural problems and one
follow-through item gate further profit and scaling:

1. **Availability** — host was off ~5.7 of last 7 days; nothing alerted (in-stack
   watchdog dies with the host).
2. **Trapped capital** — ~$86 (22% of bankroll) stuck in 11 open SELL positions,
   several resolved 2+ weeks ago but never booked/redeemed.
3. **Monoculture** — `whale_entry_filtered SELL` = 76% of all realized PnL.
4. **Calibration follow-through** — per-slice calibration just wired in; needs to
   be confirmed converging before it can replace the multiplier band-aid.

Legend: **[me]** = I can implement · **[you]** = needs your action · effort S/M/L.

---

## Phase 0 — Stabilize (this week)

### 0.1 Finish the dead-man's switch  · [you] · S · DONE (code)
Create a free **healthchecks.io** check (period 5m, grace 10m, attach an
email/SMS/Telegram alert), put its URL in `.env` as `HEARTBEAT_PING_URL`, then
`docker compose up -d watchdog`.
**Success:** kill the stack locally → external alert fires within ~15 min.

### 0.2 Redeem trapped positions + Gamma-independent resolution  · [me] · M
Build `scripts/redeem_resolved_positions.py`: for each open live position, detect
resolution **without** Gamma (which delists resolved markets) — use on-chain UMA
status / CLOB price 0-or-1 / data-API positions — then redeem NO tokens on-chain
and book `realized_pnl`. Wire a nightly task. Frees ~$86 and stops the recurring
limbo leak (inverse of the old phantom-loss bug).
**Success:** the 4 past-resolution positions (#16210, #22994, #32224, #30316)
close with correct PnL; open-position capital drops to only genuinely-live markets.

### 0.3 Verify the calibration fix is converging  · [me] · S
Over the next 2 weekly refits, confirm `signal_sizing_multipliers` drift toward
1.0 (now that confidence is per-slice calibrated) and Brier on newly-resolved
live trades sits <0.25. Add a one-line "calib vs multiplier" delta to the daily
review so divergence is visible.
**Success:** multipliers move 2.0×/1.83× → toward 1.0×; no Brier regression.

---

## Phase 1 — Recover & de-risk (next ~2 weeks)

### 1.1 Break the monoculture  · [me] · M
`whale_entry_filtered SELL` is 76% of PnL — single point of failure. Promote the
next signal that passes the gates (n≥50 paper, WR≥45%, ex-top-3 EV>+$0.25):
- `cascade SELL` (90d WR 75% but n=4 — needs sample) — closest candidate
- `oversized_bet SELL` (WR 62%, n=13, +$13.92) — grow to n≥50, then size up
Broaden 1–2 *proven* (signal × category) slices beyond politics SELL.
**Success:** top single slice < 50% of |realized PnL| in the daily attribution.

### 1.2 Fix the specialist_entry SELL leak  · [me] · S
It's the only SELL signal with a bad win/loss ratio (avgWin 2.79 / **avgLoss
−1.91**, WR 53%, +$9.86 net). Investigate exit/stop behaviour — its losers run
too far vs the other SELL signals. Tighten the stop or add a time-box.
**Success:** avgLoss for specialist SELL pulls in toward the −0.7 to −0.8 band of
the healthy SELL signals.

### 1.3 Decide on always-on hosting  · [you] · M
The heartbeat (0.1) tells you when you're down; it doesn't keep you up. The real
fix is moving `scheduler` + `live-collect` + `postgres` to a cheap always-on box
(small VPS, ~$5–10/mo). At $386 bankroll growing 40%/cycle, a week of uptime is
worth more than the hosting cost. Decision: migrate now, or wait until ~$1k.
**Success:** uptime no longer depends on your home PC being awake.

---

## Phase 2 — Scale readiness (this month+)

Per the scaling-readiness criteria, advancing past 1.5× bankroll needs:

### 2.1 Sample to n≈500 resolved live trades  · time (~40 days at current pace)
### 2.2 Hold Brier < 0.25 on a rolling 30d window across all signals with n≥20
### 2.3 Retire the sizing multipliers once they've drifted to ~1.0 (post 0.3)
### 2.4 ≥2 uncorrelated live signals (depends on 1.1)

**Then** step bankroll 1.0× → 1.5× → 2.0×, re-auditing at each step. 5×+ additionally
needs decorrelated multi-strategy + paged alerting on every silent-failure class.

---

## Explicitly NOT doing (and why)

- **Re-enable whale_entry_filtered BUY** — entire +$18.85 is one +$49.90 outlier;
  ex-top-3 EV −$0.90/trade, WR 10%. −EV lottery. P3 already down-weights it.
- **New signal *types*** — we can't fully exploit the ones we have; diversify
  within proven signals first.
- **Faster execution / ML upgrades** — irrelevant at this bankroll; calibration +
  uptime + diversification are the binding constraints.

---

## Suggested order

`0.1 (you)` → `0.2` → `1.1` in parallel with `0.3` monitoring → `1.2` → `1.3 (you)`
→ Phase 2 gates. Phase 0 is capital + safety; Phase 1 is durability + breadth;
Phase 2 is earned scale.
